const baseUri = "https://api.lougiequisel.com";

const Constants = {
  baseUri,
  apiUri: `${baseUri}/wp-json/wp/v2`,
  jwtAuthUri: `${baseUri}/wp-json/jwt-auth/v1/token`
};

export default Constants;
